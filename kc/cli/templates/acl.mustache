# KeepCloud (kc) protect locations using
# HTTP authentication || IP address
satisfy any;
auth_basic "Restricted Area";
auth_basic_user_file htpasswd-kc;
# Allowed IP Address List
allow 127.0.0.1;
allow ::1;
deny all;
